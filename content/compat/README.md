# Compatibility patches

Add Content Patcher patches here for other mods, using `When: { "HasMod": "Author.ModId" }`.

Examples to implement later:

- Generic Mod Config Menu (`spacechase0.GenericModConfigMenu`)
- Better Crafting
- Bigger Backpack

Each patch should only reference assets under `assets/ui/` generated locally or original art contributed to this repo.
